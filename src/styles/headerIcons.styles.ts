import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  issueIndicator: {
    width: 32,
    height: 32,
    borderRadius: 16,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 4,
  },
  issueCount: {
    fontSize: 12,
    fontWeight: '600',
    color: '#FFFFFF',
  },
});
